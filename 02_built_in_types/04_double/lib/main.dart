import 'dart:math';

void main() {
  double d = -44.6;

  /** double Attributes */
  // print(d.isNaN); // NaN = not a number (eg. Square root of a negative numbe)
  // print(d.sign);
  // print(d.isFinite);
  // print(d.isNegative);

  /** double Methods */

  /** دوال التقريب */
  // print(d.toInt());
  // print(d.round());
  // print(d.floor());
  // print(d.ceil());
  // print(d.truncate());

  // print(d.abs());
  print(d.remainder(2)); // باقي القسمة
}
