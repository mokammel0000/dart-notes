import 'dart:io';

void main() {
  // print(Platform.environment);
  // contain all data about your working environment

  // Map<String, String> env = Platform.environment;
  // env.forEach((key, value) => print(key + ' is: ' + value));

  // print('-------------------------------------------');
  // print(Platform.numberOfProcessors);
  // print(Platform.operatingSystem);
  // print(Platform.operatingSystemVersion);
  // print(Platform.localeName); // device language
  // print(Platform.localHostname); // current user name
  // print('-------------------------------------------');

  // print(Platform.executable); // where is dart sdk installed
  // print(Platform.script); // location of this dart file
  // print(Platform.version); // version of dart sdk
  // print('-------------------------------------------');

  // print(Platform.isAndroid);
  // print(Platform.isIOS);
  // print(Platform.isWindows);
  // print(Platform.isLinux);
  // print(Platform.isMacOS);
  // print(Platform.isFuchsia); // google operating system
  // print(Platform.isAndroid);
}
