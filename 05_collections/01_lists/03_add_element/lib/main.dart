void main() {
  List<int> list = [0, 0, 10, 20, 30, 40, 50];
  // list.add(50); // add to the end of the list
  // print(list);

  // list.addAll([50, 60, 70]); // add list or set to another list
  // print(list);

  // list.insert(3, 100); // add at some index, and shift other elements
  // print(list);

  // list.insertAll(0, {15, 25, 35}); //add at some index, and shift other elements
  // print(list);
}
