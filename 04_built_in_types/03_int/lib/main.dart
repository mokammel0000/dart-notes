void main() {
  int x = 20;

  /** int Attributes */

  // print(x.hashCode);
  //  commonly used in data structures like hash tables for efficient storage and retrieval.

  // print(x.bitLength); // 100
  // print(x.sign);
  // print(x.isNegative);
  // print(x.isOdd);
  // print(x.isFinite);
  // print(x.isNaN); // NaN = not a number (sqrt(-1) is not a number)

  /** int methods */
  // print(x.abs()); // absulout
  // print(x.gcd(30)); // greatest common divisor between these 2 numbers
  print(x.compareTo(44)); // greatest common divisor between these 2 numbers
}
