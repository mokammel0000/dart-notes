void main() {
  /*
    - (unary negation)
    + (unary plus)
    ++ (increment)
    -- (decrement)
    ! (logical NOT)
 */

  // int x = 3;
  // print(x++);
  // print(++x);

  // print(x--);
  // print(--x);

  // Unary negation (-)
  // x = -x;
  // print(x);

  // x = -x;
  // print(x);

  bool isPremium = true;
  bool isPoor = !isPremium;

  print(isPoor);
}
