void main() {
  /*
    bitwise operators are used to perform operations at the bit level on integer types (int)
    & (bitwise AND)
    | (bitwise OR)
    ^ (bitwise XOR)
    ~ (bitwise NOT)
    << (left shift)
    >> (right shift)
  */
}
